'use strict'

/**
 * Step 1 => User rolls dice
 * Step 2 => Display dice
 * Step 3 => If it is one pass to the other user and corrent score = 0 
 * Step 4 => If it is not one then keep adding it to the current score
 * Step 5 => If the user presses hold then pass it to the other user and add the current score to total score of user
 * Step 6 => Adding the functionality of play again
 * Step 7 => We can also add the functionality of high score if we want to add though it is not necessary
 * Step 8 => Intialize the score to 0 for both the player
 */

